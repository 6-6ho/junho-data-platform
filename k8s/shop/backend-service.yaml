apiVersion: v1
kind: Service
metadata:
  name: shop-backend
  namespace: shop
  labels:
    app: shop-backend
spec:
  selector:
    app: shop-backend
  ports:
  - port: 8000
    targetPort: 8000
    name: http
