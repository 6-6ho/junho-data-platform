apiVersion: v1
kind: Service
metadata:
  name: shop-analytics
  namespace: shop
  labels:
    app: shop-analytics
spec:
  selector:
    app: shop-analytics
  ports:
  - port: 80
    targetPort: 80
    name: http
