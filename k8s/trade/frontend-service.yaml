apiVersion: v1
kind: Service
metadata:
  name: trade-frontend
  namespace: trade
  labels:
    app: trade-frontend
spec:
  selector:
    app: trade-frontend
  ports:
  - port: 80
    targetPort: 80
    name: http
