services:
  # Override frontend to use custom Nginx config and Certbot volumes
  frontend:
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./deploy/landing:/usr/share/nginx/html/landing:ro
      - ./deploy/certbot/conf:/etc/letsencrypt
      - ./deploy/certbot/www:/var/www/certbot
    restart: unless-stopped

  # Certbot service for SSL
  certbot:
    image: certbot/certbot
    volumes:
      - ./deploy/certbot/conf:/etc/letsencrypt
      - ./deploy/certbot/www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"

  # Ensure other services restart automatically
  api:
    restart: unless-stopped
  
  ingest:
    restart: unless-stopped
    
  spark:
    restart: unless-stopped

  # Cloudflare Tunnel (directory mount to prevent file/dir issues)
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: trade-helper-tunnel
    restart: unless-stopped
    command: tunnel --config /etc/cloudflared/config.yml run
    volumes:
      - ./deploy/cloudflared:/etc/cloudflared:ro
    networks: [appnet]


