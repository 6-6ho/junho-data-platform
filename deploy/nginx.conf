server {
    listen 80;
    server_name junho.in;

    # Certbot이 SSL 인증서를 발급할 때 사용하는 경로
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # 모든 HTTP 요청을 HTTPS로 리다이렉트 (SSL 적용 후 주석 해제)
    # location / {
    #     return 301 https://$host$request_uri;
    # }

    # 임시: SSL 적용 전에는 그냥 HTTP로 서비스
    location / {
        proxy_pass http://frontend:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /api/ {
        proxy_pass http://api:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    location /grafana/ {
        proxy_pass http://grafana:3000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

# SSL 설정 (Certbot 실행 후 활성화될 부분 미리 작성)
# server {
#     listen 443 ssl;
#     server_name junho.in;
#
#     ssl_certificate /etc/letsencrypt/live/junho.in/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/junho.in/privkey.pem;
#
#     location / {
#         proxy_pass http://frontend:3000;
#         ...
#     }
#     ...
# }
